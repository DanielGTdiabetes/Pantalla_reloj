#!/usr/bin/env bash
# Pantalla_reloj — autostart fijo para kiosk Firefox

xset -dpms; xset s off; xset s noblank
xrandr --output HDMI-1 --rotate left --primary
sleep 2
setsid -f /usr/local/bin/firefox --no-remote --kiosk http://127.0.0.1 &
